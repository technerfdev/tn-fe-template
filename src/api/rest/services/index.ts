/**
 * REST API Services
 * Import services from here. Each service handles one resource.
 * Example: import { authService, userService } from '@/api/rest/services';
 */
export { authService } from './auth.service';
export { userService } from './user.service';
