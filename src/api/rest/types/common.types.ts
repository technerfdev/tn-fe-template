/**
 * REST API Common Types
 * Add REST-specific types here that aren't in the global types
 */

/**
 * Example: Resource-specific request/response types can go here
 */
export interface ExampleDto {
  // Add specific DTOs as needed
}
